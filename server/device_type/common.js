const SUPPORTED_DEVICE_TYPES = {
	ANDROID: 'android',
	IPHONE: 'iphone',
	MAC: 'mac',
	WINDOWS: 'windows',
}

const SUPPORTED_LANGUAGES = {
	KOREAN: "KOR",
};

module.exports = { SUPPORTED_DEVICE_TYPES, SUPPORTED_LANGUAGES };